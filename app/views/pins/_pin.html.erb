<div class="box">
	<h4> <%= pin.title %> </h4> 
<p><%= pin.description %></p>
<p>
	<strong>
		Posted by <%= pin.user.name %>
	</strong>
<p>
<% if current_user == pin.user %>
	<%= link_to 'Edit', edit_pin_path(pin) %>
	<%= link_to 'Destroy', pin, method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% end %>
</div>